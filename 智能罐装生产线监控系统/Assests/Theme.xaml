<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:智能罐装生产线监控系统.ViewModel"
                    xmlns:views="clr-namespace:智能罐装生产线监控系统.View">
    

    <DataTemplate DataType="{x:Type vm:DashBoardViewModel}">
        <views:DashBoardView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:AlarmsViewModel}">
        <views:AlarmsView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:DashQueryViewModel}">
        <views:DashQueryView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:LogsViewModel}">
        <views:LogsView />
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:SettingViewModel}">
        <views:SettingView />
    </DataTemplate>


    <!--主应用深色背景-->
    <SolidColorBrush x:Key="AppBgDark" Color="#1E1E1E"/>
    <!--面板，内容背景-->
    <SolidColorBrush x:Key="AppBgPanel" Color="#252526"/>
    <!--Tab,Header背景-->
    <SolidColorBrush x:Key="AppBgHeader" Color="#2D2D30"/>
    <!--主按钮，选中导航，边框背景-->
    <SolidColorBrush x:Key="AppAccentColor" Color="#007ACC"/>

    <!--主要文字颜色：正文，标题背景-->
    <SolidColorBrush x:Key="AppTextMain" Color="#FFFFFF"/>
    <!--次要文字背景，占位文本背景-->
    <SolidColorBrush x:Key="AppTextMuted" Color="#AAAAAA"/>
    <!--成功：显示成功，正常状态背景-->
    <SolidColorBrush x:Key="AppSuccess" Color="#4CAF50"/>
    <!--信息颜色：用于信息提示背景-->
    <SolidColorBrush x:Key="AppInfo" Color="#4CAF50"/>
    <!--警告颜色背景-->
    <SolidColorBrush x:Key="AppWarning" Color="#FF9800"/>
    <!--错误危险颜色背景-->
    <SolidColorBrush x:Key="AppDanger" Color="#D50000"/>
    <!--通用边框背景：控件边框，card背景-->
    <SolidColorBrush x:Key="AppBorderBrush" Color="#3E3E42"/>
     <!--导航栏样式-->
    <Style x:Key="NavigItemStyle" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{StaticResource AppTextMuted}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="3,0,0,0"/>
        <Setter Property="Padding" Value="10,15"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <!--绑定的作用就是让触发器能够改变该Border-->
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3E3E42"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background" Value="#37373D"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="{StaticResource AppAccentColor}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>
    <!--通讯设置区域背景-->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AppBgDark}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="15"/>
    </Style>
    <!--日志标题样式设置-->
    <Style x:Key="TabItem11" TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource AppTextMuted}"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Margin" Value="2,0,2,0"/>
        <Setter Property="MinWidth" Value="64"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="bd" BorderBrush="Transparent" Background="Transparent" BorderThickness="0" CornerRadius="6,6,0,0">
                        <ContentPresenter ContentSource="Header" Margin="10,5"/>
                        <!--绑定父控件的依赖属性Header-->
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Background" Value="#777777"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource AppBgHeader}" />
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AppAccentColor}" />
                            <Setter  Property="Foreground" Value="{StaticResource AppTextMain}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#2E2E2E" />
                            <Setter Property="Foreground" Value="{StaticResource AppTextMain}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--日志内容样式-->
    <Style x:Key="TabControlContext" TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource AppBgDark}"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource TabItem11}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Border Background="Transparent" Padding="6,6,6,0">
                            <!--意思是“所有 TabItem 的头部（Header）都自动放到这个面板里-->
                            <TabPanel IsItemsHost="True" Name="HeaderPanel" Margin="1"/>
                        </Border>
                        <Border Grid.Row="1" Background="{StaticResource AppBgPanel}" 
                                BorderBrush="{StaticResource AppBorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="0,6,6,6" 
                                Margin="0,8,0,0" 
                                Padding="{TemplateBinding Padding}" >
                            <ContentPresenter x:Name="PART_SelectedContentHost"  ContentSource="SelectedContent" Margin="8"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
      
</ResourceDictionary>